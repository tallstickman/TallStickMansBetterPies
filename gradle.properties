# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Allow Gradle to automatically download a matching JDK for the toolchain when configured.
org.gradle.java.installations.auto-download=true

# Note: Gradle wrapper 8.8 supports Java 21 toolchains. If you encounter issues, update
# the Gradle wrapper (./gradlew wrapper --gradle-version 8.8 or newer).

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21
yarn_mappings=1.21+build.9
loader_version=0.17.3

# Mod Properties
mod_version=1.1.1
maven_group=tv.tallstickman.betterpies
archives_base_name=TallStickMans_BetterPies

# Dependencies

# Fabric API
fabric_version=0.102.0+1.21

loom_version=1.11-SNAPSHOT

